# archived — 重寫前舊原型（已凍結）

← [INDEX](../../INDEX.md)

本目錄是專案**重寫前**的原型碼與草稿，非現役 `medp` 函式庫源碼；保留只為查閱早期設計脈絡，不在維護鏈內，也不套用結構整理門檻。

| 內容 | 說明 |
|------|------|
| `*.js`（advanced_systems、deep_systems、rpg_magic_elements、strategy_game_system…）| 早期玩法/系統原型（JS）|
| `*.cpp`（sdl_map_generator、sdl_tilemap）| 早期 SDL 地圖/tilemap 原型 |
| `sql1/`（game.cpp、tilemap.cpp）| 另一版 C++ 原型 |
| `gd/`（`.gd` 腳本 + scene 導引 `.md`）| Godot 前端場景草稿 |
| `a.txt`、`struc.txt` | 早期文字草稿 |

現役程式碼導航見 [CODE_MAP](../../workflows/common/code-map/CODE_MAP.md)。
